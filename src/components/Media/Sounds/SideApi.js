import React, { useEffect, useState,useContext } from 'react';
import { UserContext } from '../../Context/UserContext'

export default function SideApi({ setSide }) {
    const { baseURL } = useContext(UserContext);

   
    return null;
}
